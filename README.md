# graph-library
## ЗВІТ

Розподіл роботи та написані алгоритми:
1. **Настя Квасовець**

Читання графу з файлу - функції, які вміють прочитати файл і записати дані в відповідну структуру. Формат файлу - csv, перша колонка містить перші вершини кожного ребра, друга - другі вершини (порядок вершин не грає ролі в випадку неорієнтованого графу, і є важливим коли граф орієнтований).

На основі вмісту файлу функції будують:
список суміжності (словник, де кожній вершині відповідає множина сусідів);
множину ребер (пари вершин).

Це дозволяє легко працювати з графами в програмі: аналізувати зв’язність, знаходити шляхи, компоненти тощо.

Вхідні параметри:
filename: str -> Шлях до CSV-файлу. Кожен рядок повинен мати вигляд NodeA,NodeB.
oriented: bool -> Визначає тип графа. Якщо True – граф орієнтований; якщо False – неорієнтований (за замовчуванням).

Функція read_graph_from_csv_to_set повертає:
edges: set[tuple[str, str]] — множина ребер у вигляді кортежів (source, destination).
Для неорієнтованого графа кожне ребро додається в обох напрямках.

Алгоритм роботи
Ініціалізація структури даних:
edges = set()

Зчитування рядків файлу з кодуванням UTF-8.

Обробка кожного рядка:
Видалення пробілів (strip()).
Розділення за комою (split(',')).
Перевірка, що в рядку рівно два вузли. Якщо ні — видання вказівного повідомлення.

Додавання ребра (NodeA → NodeB):
додавання кортежу у edges.

Якщо граф неорієнтований (oriented == False):
додавання відповідного ребра в множину.

Повернення заповненої структури.

Функція read_graph_from_csv_to_dict повертає:
connections: dict[str, set[str]] — список суміжності;

Алгоритм роботи
Ініціалізація структури даних:
connections = {}

Зчитування рядків файлу з кодуванням UTF-8.

Обробка кожного рядка:
Видалення пробілів (strip()).
Розділення за комою (split(',')).
Перевірка, що в рядку рівно два вузли. Якщо ні — видання вказівного повідомлення.

Додавання ребра (NodeA → NodeB):
оновлення connections;

Якщо граф неорієнтований (oriented == False):
додавання зворотного зв’язку (NodeB → NodeA);

Повернення заповненої структури.

Функції ігнорують:
порожні рядки;
рядки з неправильним форматом (не 2 значення);
рядки з зайвими комами.

Це робить функції стійкішими до помилок у файлі.

Переваги реалізації:
Підтримка орієнтованих і неорієнтованих графів.
Автоматичний захист від некоректних рядків.
Ефективні структури даних (dict, set).
Проста інтеграція в більші проєкти.

2. **Остап Сухомлин**

Функція приймає граф та повертає Гамільтоновий цикл (чи повідомлення про його відсутність) у формі списку вершин. Реалізовано функцію `make_way`, що виконує пошук Гамільтонового циклу в графі за допомогою рекурсивного backtracking. Алгоритм послідовно будує шлях, додаючи сусідні вершини, які ще не були відвідані, та перевіряє можливість замкнення циклу поверненням до стартової вершини.

Функція:

* приймає граф у вигляді списку суміжності;
* перебирає всі можливі стартові вершини;
* формує шлях, уникаючи повторних відвідувань;
* при досягненні довжини `n` вершин повертає цикл `path + [path[0]]`;
* повертає `False`, якщо цикл неможливо побудувати.

Також додано низку докладних доктестів у документації функції: графи з циклами, орієнтовані графи, дерева, графи з петлями й ізольованими вершинами, а також великі цикли. Це дозволило впевнитися в коректності реалізації в різних випадках.

Реалізація вийшла лаконічною, зрозумілою й легко інтегрується в загальний проєкт бібліотеки графів.



3. **Марія-Дарина Загарюк**

Функція приймає граф та повертає Ейлерів цикл (чи повідомлення про його відсутність) у формі списку вершин.

Реалізація:
функція find_euler_cycle(graph, orientated)
Визначення: Ейлерів цикл — це замкнений шлях у графі, який проходить через кожне ребро рівно один раз і повертається у початкову вершину.
Критерії існування (Теорема Ейлера-Гіргольцера): 1) граф зв'язний (усі ребра належать одній компоненті зв'язності); 2) для орієнтованого: вхідний степінь == вихідний степінь; для неорієнтованого: степінь кожної вершини парний.
Якщо ці умови не виконуються, функція повертає None.

Побудова Ейлерового циклу: обрано **алгоритм Гіргольцера**.
Чому саме цей алгоритм?
Він є ефективнішим за алгоритм Флері, адже його часова складність складає $O(E)$, де E — кількість ребер.

Детальніше про перевагу алгоритму Гіргольцера:
На відміну від алгоритму Флері, який на кожному кроці вимагає перевірки ребра на те, чи є воно "мостом" (що потребує запуску алгоритму пошуку в ширину/вглиб і збільшує часову складність до O(E^2)), алгоритм Гіргольцера не виконує попередніх перевірок. Ефективність O(E) досягається завдяки тому, що:
Кожне ребро графа розглядається та додається до стека рівно один раз.
Кожне ребро видаляється з графа рівно один раз.
Це робить алгоритм Гіргольцера лінійним відносно кількості ребер, що дозволяє використовувати його для побудови циклів у графах з сотнями тисяч ребер за частки секунди.

Детальніше про роботу алгоритму Гіргольцера:
Алгоритм реалізовано ітеративним методом з використанням стека LIFO.
Алгоритм будує Ейлерів цикл, поступово знаходячи та об'єднуючи підцикли. Він використовує стек для відстеження поточного шляху та список для запису результату.

Основні кроки:
Рух вперед (DFS): Починаємо зі стартової вершини. Рухаємося по ребрах вглиб, додаючи вершини у stack і одразу видаляючи пройдені ребра з графа.

Крок назад: Якщо в поточної вершини немає доступних ребер (зайшли в глухий кут), переносимо її зі stack у фінальний список circuit. Це означає, що всі шляхи з цієї точки вже пройдені.

Циклічність і фінал: Повторюємо процес, доки стек не спорожніє. Отриманий список circuit перевертаємо, щоб отримати правильний порядок обходу.


4. **Аліна Яцко**

Під час тестування функції is_bipartite було перевірено її роботу на різних типах графів: порожньому, з одним вузлом, ланцюгах, парних та непарних циклах, а також орієнтованих графах. У всіх випадках функція коректно будувала неорієнтоване подання графа та виконувала алгоритм перевірки двочастковості, який базується на BFS-розфарбуванні у два кольори. Під час обходу кожна вершина отримувала колір, а її сусіди — протилежний. Якщо алгоритм виявляв двох сусідів з однаковим кольором, робився висновок, що граф не є двочастковим. Усі тестові приклади завершилися успішно, що підтверджує правильність реалізації BFS-алгоритму та побудови underlying-графа.

Функція three_coloring також пройшла всі тестові сценарії. Її робота ґрунтується на алгоритмі DFS із поверненням (backtracking), який намагається послідовно призначити кожній вершині один з трьох кольорів ('r', 'b', 'g'). Для кожної вершини алгоритм перевіряє, чи не конфліктує вибраний колір із кольорами сусідів; у разі конфлікту відбувається повернення (відкат) та вибір іншого кольору. Якщо жоден колір не підходить, алгоритм повідомляє, що 3-розфарбування неможливе. Тестування підтвердило, що функція правильно формує underlying-граф, коректно перевіряє сумісність кольорів та надійно знаходить розфарбування там, де воно існує.


5. **Софія Вольвач**

Функція are_isomorphic(graph1, graph2) приймає два графи та перевіряє їх на ізоморфізм за допомогою Weisfeiler-Lehman test.

Тест ітерується по вершинам обох графів на присвоює їм "кольори" (унікальний хеш), залежно від кількості суміжних вершин і їх кольорів. Потім списки цих кольорів порівнюються, і якщо вони рівні, то функція поверне значення True. Тесту не на 100% точний, і може повернути True навіть для неізоморфних графів, але протилежне неможливо.

Для пришвидшення роботи функції спочатку порівнюється кількість вершин. Якщо вона однакова, для обох графів запускається функція hash_wl(graph), яка повертає відсортований список унікальних хешів для кожної вершини. За початковий "колір" кожної вершини встановлюється вихідний степінь, далі виконуються 3 ітерації оновлення кольору. Для кожної вершини збираються поточні кольори вхідних та вихідних суміжних вершин і хешуються за допомогою бібліотеки hashlib. Після цих ітерацій "кольори" усіх вершин збираються у список, які потім сортуються. Списки обох графів порівнюються, і якщо вони рівні, то для кожної вершини з першого графу можна знайти відповідну вершину з другого. Але деякі неізоморфні графи можуть мати таку структуру, що пройдуть цей тест.

## Як встановити нашу бібліотеку?

1. Клонуємо наш репозиторій для нового користувача:
Пишемо в терміналі: git clone https://github.com/zahariukpn-cs/graph-library.git

2. Відкриваємо папку з нашою бібліотекою:
cd GRAPH-LIBRARY

3. Створюємо віртуальне середовище:
python -m venv venv

4. Активуємо це середовище:
.\venv\Scripts\activate для Windows
source venv/bin/activate для MacOS і Linux

5. Встановлюємо потрібні бібліотеки:
pip install -r requirements.txt

6. Щоб почати використовувати функції нашої бібліотеки, створюємо новий Python-файл у кореневій директорії проєкту 'GRAPH-LIBRARY'.

7. Імпортування функцій із нашої бібліотеки:
1) Читання графу й записування в словник:
from read_graph_from_csv import read_graph_from_csv_to_dict
Читання графу й записування в множину:
from read_graph_from_csv import read_graph_from_csv_to_set
2) Гамільтоновий цикл:
3) Ейлерів цикл:
from euler_cycle import find_euler_cycle
4) Перевірка на дводольність:
from graph_painting import is_bipartite
6) Перевірка на ізоморфність:
from isomorphism import are_isomorphic
7) Розмалювання графу в 3 кольори:
from graph_painting import three_coloring

## Інтерфейс роботи через командний рядок із використанням бібліотеки argparse

1. Читання файлу:
python main.py graph.csv --show
2. Реалізація Ейлерового циклу:
python main.py graph.csv --euler
3. Реалізація Гамільтонового циклу:
python main.py graph.csv --hamilton
4. Перевірка на дводольність:
python main.py graph.csv --bipartite
5. Розфарбування графа:
python main.py graph.csv --coloring
6. Перевірка на ізоморфність:
python main.py graph1.csv --isomorph --file2 graph2.csv (для ізоморфності необхідно вказати 2 файли)
7. Інструкції для роботи з командним рядком:
python main.py --help
Якщо граф орієнтований, то після виклику "python main.py" треба вказати --oriented будь-де.

## Висновки

У ході виконання цього проєкту наша команда розробила Python-бібліотеку для роботи з графами. Головним результатом стала реалізація argparse, що дозволяє аналізувати структуру графів, зчитуючи їх безпосередньо з CSV-файлів, і візуалізація через streamlit, що виглядає як реальний комерційний продукт.

**Ключові технічні досягнення:**

1.  **Вигляд і структура:**
    * Ми побудували модульну систему, де кожен алгоритм винесено в окремий файл, а `main.py` виступає єдиною точкою входу.
    * Використання бібліотеки `argparse` дозволило створити зручний інтерфейс командного рядка, наблизивши проєкт до стандартів реальної розробки програмного забезпечення.

2.  **Алгоритмічна ефективність:**
    * Для пошуку **Ейлерового циклу** ми свідомо обрали **алгоритм Гіргольцера**. Це дозволило досягти лінійної часової складності $O(E)$, що є значно ефективнішим за алгоритм Флері (O(E^2)).
    * Для перевірки **ізоморфізму** використано хешування за **алгоритмом Вейсфейлера-Лемана (1-WL)**. Це дозволило уникнути факторіальної складності повного перебору перестановок і зробити перевірку швидкою для більшості випадків.

3.  **Робота з даними:**
    * Реалізовано універсальний парсер CSV-файлів, який коректно обробляє як орієнтовані, так і неорієнтовані графи, та захищений від помилок форматування вхідних даних.

---

##  Враження від виконання

Виконання проєкту стало чудовим способом "оживити" суху теорію та перевірити (засумніватись у) свої навички програмування.

* **Від теорії до практики:** Одне діло — малювати графи в зошиті, і зовсім інше: дебажити рекурсію у пошуку розфарбування або розбиратися, чому цикл не замикається. Це дало глибоке розуміння того, як працюють структури даних насправді.
* **Командна робота (Git):** Проєкт став чудовим тренуванням культури коду. Ми навчилися вирішувати конфлікти, домовлятися про формати даних (наприклад, узгодження повернення `dict` чи `set`), читати чужий код і розподіляти відповідальність, а дехто повчився не ігнорувати інших.
* **Найцікавіший виклик:** Найскладнішим, але й найцікавішим, було написання універсального зчитування файлів так, щоб це не зламало логіку роботи алгоритмів інших учасників команди, а також налагодження комунікації.

---

## Фідбек викладачам та асистентам ❤️❤️❤️❤️❤️

Дякуємо за можливість вибору теми і цікавий проєкт! Це завдання стало чудовим містком між теорією та практикою програмування. Завдяки формату бібліотеки ми прокачали вміння писати модульний код, а можливість самостійно формувати набір алгоритмів дозволила краще проявити сильні сторони кожного учасника команди.

**Окрема подяка:**

Хочемо висловити величезну вдячність нашому ментору.

> **Назар — найкращий ментор!** 🔥

Дякуємо за:
* Швидкі та змістовні відповіді.
* Допомогу в розумінні (не)очевидних деталей.
* Підтримку починаючи з перших днів початку проєкту.

Без якісного менторства розібратися з тонкощами реалізації алгоритмів, git hub, argparse, візуалізації було б значно важче.
